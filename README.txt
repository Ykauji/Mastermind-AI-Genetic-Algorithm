TO RUN:
    make sure "Mastermind for students.lisp" and "basic search engine" is compiled before running
    1) compile PathFinders.lisp
    2) functions calls to execute tournament can be found at the end of PathFinders.lisp
        a) you can change the SCSA arugment in (play-tournament *Mastermind* 'PathFinders SCSA rounds)